server:
  port: 8080
  context-path: /eureka

spring:
  application:
    name: eureka
  profile:
    active: local

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    waitTimeInMsWhenSyncEmpty: 0

security:
  basic:
    enabled: true
  user:
    name: test-eureka
    password: test-password